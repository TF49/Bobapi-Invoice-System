# 发票申请批量导入实施计划

> 编制日期：2026-08-19  
> 适用范围：普通用户批量创建发票申请  
> 不包含：管理员批量上传发票图片；管理员仍按现有流程逐张上传

## 1. 目标与交付结果

在用户端“提交发票申请”区域增加批量导入能力。用户上传包含公司名称、税号、开票金额的 CSV 或 XLSX 文件，系统完成预览、逐行校验、用户确认和批次提交，并返回每一行对应的处理结果。

交付后应满足：

- 用户可以下载标准导入模板。
- 用户可以导入 CSV、XLSX 文件，预览解析后的数据。
- 非法行在提交前明确显示行号和错误原因。
- 后端再次执行完整参数校验，不能信任前端校验结果。
- 一批数据最多 100 行，确认后以一个批次提交。
- 批次采用全有或全无语义：校验通过后全部创建成功，系统异常时全部回滚。
- 网络重试不会重复创建发票申请。
- 批量创建的记录状态仍为 `PENDING`，管理员继续逐张上传图片。
- 原有单条申请接口和管理员图片上传流程保持兼容。

## 2. 当前代码基线

| 模块 | 当前实现 | 批量功能影响 |
| --- | --- | --- |
| 用户页面 | [frontend/src/views/UserInvoice.vue](frontend/src/views/UserInvoice.vue) 只提交一条申请 | 增加批量导入入口、预览表和结果弹窗 |
| 前端 API | [frontend/src/api/invoice.ts](frontend/src/api/invoice.ts) 只有 `createInvoice` | 增加 `createInvoicesBatch`、模板下载或生成工具 |
| 后端控制器 | `POST /api/invoices` 接收单个 `InvoiceRequest` | 增加 `POST /api/invoices/batch` |
| 后端服务 | `InvoiceService.createInvoice` 单条创建并使用幂等键 | 增加批次校验、事务创建和批次幂等 |
| 数据库 | `invoice` 表有 `(user_id, idempotency_key)` 唯一索引 | 增加批次表，并将发票关联到批次 |
| 限流 | 用户每分钟最多创建 10 次单条申请 | 增加批次次数和批次行数限制 |
| 前端依赖 | 未安装 Excel 解析库 | 增加 `xlsx`，CSV 使用同一套表格解析逻辑或轻量解析器 |

## 3. 范围决策

### 3.1 文件格式

第一版支持：

- `.xlsx`：Excel 工作簿的第一个工作表。
- `.csv`：UTF-8，允许 UTF-8 BOM。

第一版不支持：`.xls`、多工作表合并、图片附件、自动 OCR、从文件中读取用户 ID 或发票状态。

### 3.2 模板字段

模板固定为三列，第一行为表头：

| 列名 | 类型 | 约束 | 示例 |
| --- | --- | --- | --- |
| 公司名称 | 字符串 | 必填，去除首尾空白，最长 200 字符 | 示例公司 |
| 税号 | 字符串 | 必填，转大写后匹配 `[A-Z0-9]{15,20}` | 91500123456789012A |
| 开票金额 | 数字/字符串 | 必填，`0.01` 以上，最多 10 位整数和 2 位小数 | 1000.00 |

表头默认严格匹配“公司名称、税号、开票金额”。不通过模糊字段名猜测，避免用户误把其他列导入。

### 3.3 批次语义

采用“预览通过后整体提交”的全有或全无语义：

- 空行可以忽略；有数据的行必须全部合法。
- 任意一行错误时，不创建任何发票申请。
- 数据库异常时，事务整体回滚。
- 同一批次重复请求返回第一次批次的结果。
- 同一幂等键提交不同内容时返回冲突，不创建新数据。

不自动拦截历史上相同公司、税号和金额的申请，因为同一用户可能对同一公司发起多次真实开票。只拦截同一文件内的完全重复行，并给出重复行号。

## 4. 用户端交互设计

### 4.1 入口

在现有“提交发票申请”面板增加两个操作：

- `下载导入模板`
- `批量导入`

保留现有单条表单，用户可以继续使用原有提交方式。批量导入使用独立弹窗或抽屉，不与单条表单共享提交状态。

### 4.2 导入流程

1. 用户下载模板或选择已有 CSV/XLSX 文件。
2. 前端检查扩展名、文件大小和文件是否可解析。
3. 读取第一个工作表，识别表头并转换为标准行对象。
4. 展示预览表：原始行号、公司名称、税号、金额、校验状态、错误原因。
5. 前端执行快速校验，后端在确认提交时再次完整校验。
6. 全部通过后，用户点击“确认导入”。
7. 显示提交进度和最终结果：批次号、成功条数、总金额、申请 ID 范围。
8. 成功后刷新用户发票列表；失败时保留预览数据和错误信息，允许修改文件后重新导入。

### 4.3 前端状态

建议明确区分以下状态：

- `IDLE`：未选择文件。
- `PARSING`：正在读取文件。
- `PREVIEW`：已解析，等待确认。
- `SUBMITTING`：批次提交中，禁止重复确认。
- `SUCCESS`：批次成功。
- `ERROR`：解析、校验或提交失败。

网络错误时不要直接清空批次幂等键。用户重试时复用同一幂等键和同一规范化数据；如果后端已成功，后端应返回原批次结果。

## 5. 后端接口设计

### 5.1 批量创建接口

```http
POST /api/invoices/batch
Authorization: Bearer <JWT>
Idempotency-Key: <16-64 位批次幂等键>
Content-Type: application/json
```

请求体：

```json
{
  "items": [
    {
      "companyName": "示例公司A",
      "taxNumber": "91500123456789012A",
      "amount": "1000.00"
    },
    {
      "companyName": "示例公司B",
      "taxNumber": "91500123456789013B",
      "amount": "2500.50"
    }
  ]
}
```

响应体：

```json
{
  "code": 200,
  "message": "批量申请成功",
  "data": {
    "batchId": 3001,
    "total": 2,
    "successCount": 2,
    "failureCount": 0,
    "totalAmount": "3500.50",
    "items": [
      {
        "rowNumber": 2,
        "invoiceId": 101,
        "status": "SUCCESS",
        "message": "申请成功"
      },
      {
        "rowNumber": 3,
        "invoiceId": 102,
        "status": "SUCCESS",
        "message": "申请成功"
      }
    ]
  },
  "traceId": "..."
}
```

### 5.2 建议增加的 DTO

- `BatchInvoiceRequest`：包含 `items`，限制 1～100 条。
- `BatchInvoiceItemRequest`：复用 `InvoiceRequest` 的字段约束，必要时增加 `rowNumber` 仅用于错误定位，不允许客户端传用户 ID。
- `BatchInvoiceResponse`：批次 ID、总条数、成功/失败条数、金额汇总、逐行结果。
- `BatchInvoiceItemResult`：源文件行号、发票 ID、状态、消息。
- `BatchValidationError`：行号、字段名、错误码、用户可读消息。

### 5.3 错误返回

建议使用结构化错误，不只返回一条字符串：

- `40001`：请求体不是合法 JSON、缺少 `items`、超过 100 行。
- `40003`：表头或字段格式不合法。
- `40902`：批次幂等键已用于另一份数据。
- `42202`：批次存在业务校验错误，响应中携带行号和字段错误列表。
- `42902`：批次提交过于频繁。
- `50000`：系统异常，事务回滚，返回统一错误消息和 `traceId`。

需要扩展现有统一异常处理，使 `42202` 可以携带错误明细，同时不泄露 SQL、文件路径或内部堆栈。

## 6. 数据库与幂等设计

### 6.1 新增批次表

建议通过 Flyway 增加迁移，例如 `V3__add_invoice_batch.sql`：

```text
invoice_batch
  id                BIGINT PK AUTO_INCREMENT
  user_id           BIGINT NOT NULL
  idempotency_key   VARCHAR(64) NOT NULL
  request_hash      CHAR(64) NOT NULL
  total_count       INT NOT NULL
  total_amount      DECIMAL(14,2) NOT NULL
  status            VARCHAR(20) NOT NULL
  created_at        DATETIME NOT NULL
  updated_at        DATETIME NOT NULL
  deleted           INT NOT NULL DEFAULT 0

UNIQUE (user_id, idempotency_key)
INDEX (user_id, created_at)
```

批次状态第一版只需要 `COMPLETED`；如果要记录异常，可增加 `FAILED`。校验错误不建议创建批次记录，避免把用户尚未确认的数据写入业务历史。

### 6.2 发票表关联批次

给 `invoice` 增加：

- `batch_id BIGINT NULL`：单条申请为空，批量申请关联批次。
- `batch_row_number INT NULL`：保存原始文件行号，便于审计和结果定位。
- 对 `batch_id` 增加普通索引。

是否增加外键需要结合现有逻辑删除策略确认；若增加外键，批次删除必须采用逻辑删除，不允许破坏历史发票归属。

### 6.3 幂等流程

1. 服务端规范化每个字段：公司名 trim、税号 trim 后转大写、金额转 `BigDecimal` 后按 2 位精度比较。
2. 按原始行顺序序列化规范化后的 items，计算 SHA-256 `requestHash`。
3. 查询当前用户和 `Idempotency-Key` 对应的批次。
4. 已存在且 hash 相同：查询关联发票，返回第一次结果。
5. 已存在但 hash 不同：返回 `40902`。
6. 不存在：在事务中创建批次和全部 invoice 记录。
7. 使用数据库唯一索引处理并发重复请求；捕获重复键后重新读取批次结果。

不要把同一个批次幂等键直接写入每一条 invoice 的 `idempotency_key`，否则会违反现有的用户+幂等键唯一索引。批量记录使用批次表隔离，单条申请继续使用现有逻辑。

## 7. API 防护六道防线

### 第一防线：参数校验

- `items` 必填，数量 1～100。
- 公司名称、税号和金额沿用 `InvoiceRequest` 的后端约束。
- 拒绝未知字段或在 JSON 映射层保持严格字段策略，避免客户端误传敏感字段。
- 限制请求体大小；建议批量 JSON 不超过 512KB。
- 对金额使用 `BigDecimal`，禁止使用浮点数进行服务端金额计算。

### 第二防线：业务规则校验

- 所有批量申请的 `userId` 从 JWT 获取，不能从请求体读取。
- 去除空行后，禁止批次内完全重复行。
- 规范化后重新检查税号和金额，不能只信任前端解析结果。
- 批次不允许包含已存在的 invoice ID 或状态字段。
- 批量创建的状态固定为 `PENDING`。

### 第三防线：权限校验

- `/invoices/batch` 要求已登录用户。
- 普通用户只能创建自己的申请。
- 管理员是否可调用该接口不新增特殊权限；如暂不需要，可在业务层按角色限制或保持普通认证即可。

### 第四防线：幂等处理

- 要求批次级 `Idempotency-Key`，格式沿用单条接口的 16～64 位规则。
- 数据库唯一索引兜底并发重复提交。
- 保存请求 hash，防止相同 Key 被用于不同文件。
- 网络失败重试返回原批次结果，不重复生成发票。

### 第五防线：请求限流

- 用户：每分钟最多 3 个批次。
- 每批最多 100 行，单用户每分钟最多创建 300 行。
- IP：增加批量接口独立计数，建议每 5 分钟最多 20 个批次。
- 限流计数按请求批次计算，不让批量接口通过一条请求绕开单条接口的防护目标。

具体阈值在压测后配置化，不硬编码在控制器中。

### 第六防线：熔断降级与统一异常

- 批次事务中的任意数据库异常都回滚。
- 不采用逐行吞异常继续写入，避免用户无法判断哪些申请已创建。
- 返回统一错误结构、业务码和 `traceId`。
- 记录批次 ID、用户 ID、行数和耗时，不记录完整税号或敏感请求内容到普通日志。
- 数据库不可用时快速失败，不排队堆积内存任务。

## 8. 前端实现任务

### 8.1 依赖与解析

- 在 `frontend/package.json` 增加 `xlsx`，锁定兼容 Vue 3/Vite 的版本。
- 新增 `frontend/src/utils/invoiceImport.ts`，负责 XLSX/CSV 读取、表头识别、行号保留和字段标准化。
- 解析器只返回普通对象，不直接调用 API，方便单元测试。
- 对文件大小、扩展名、空文件、空工作表、超过 100 行做前置拦截。

### 8.2 页面组件

建议在 `UserInvoice.vue` 中增加独立组件 `InvoiceBatchImportDialog.vue`，避免继续扩大当前页面的表单逻辑：

- 文件选择和模板下载。
- 解析状态和错误提示。
- 预览表格与行级校验结果。
- 全选/取消确认不需要，所有错误行必须先修复或移除。
- 确认提交按钮、提交中 loading、结果统计和关闭行为。

### 8.3 前端校验

- 公司名 trim 后非空且不超过 200 字符。
- 税号 trim、转大写后匹配 `/^[A-Z0-9]{15,20}$/`。
- 金额转字符串后使用十进制定点规则检查，不接受 `NaN`、负数或超过 2 位小数。
- 只在前端报告错误，不替代后端校验。
- 从用户文件中去掉空行；保留原始行号用于错误显示。

### 8.4 请求与结果

- 新增 `invoiceApi.createInvoicesBatch(items, idempotencyKey)`。
- 确认提交前生成一个批次幂等键并保存到当前导入状态。
- 请求成功后刷新 `getMyInvoices()`。
- 失败后不自动生成新 Key；只有用户更换文件或明确重置批次时才生成新 Key。
- 结果弹窗显示批次号、成功条数、总金额和各行申请 ID。

## 9. 后端实现任务

### 9.1 DTO 与校验

- 新增批次请求、响应和行错误 DTO。
- 复用或抽取单条 `InvoiceRequest` 的字段校验规则，避免单条和批量出现不同约束。
- 增加集合大小校验和批次级自定义校验器。
- 统一返回字段级、行级错误。

### 9.2 Service 与事务

- 在 `InvoiceService` 中增加 `createInvoicesBatch`。
- 拆出字段标准化和单行校验方法，供单条、批量共同使用。
- 批次服务先完成全量校验，再进入 `@Transactional` 创建阶段。
- 使用批量 insert 或 MyBatis-Plus 批量写入，避免循环产生大量往返 SQL。
- 事务成功后查询并返回完整 `InvoiceResponse` 或轻量行结果。

### 9.3 Mapper 与迁移

- 新增 `InvoiceBatch` entity、`InvoiceBatchMapper`。
- 增加批次按用户+幂等键查询、按批次 ID 查询发票的方法。
- 新增 Flyway 迁移并同步更新 `init.sql`，但不删除或重写已有迁移。
- 检查现有 baseline、逻辑删除和开发环境初始化脚本的兼容性。

### 9.4 Controller 与限流

- 新增 `@PostMapping("/batch")`。
- 继续使用 `@AuthenticationPrincipal` 获取当前用户。
- 按批次数、用户、IP执行独立限流。
- 统一处理 `Retry-After`，让前端能显示可重试时间。

## 10. 测试计划

### 10.1 前端单元测试

- 正常解析 XLSX。
- 正常解析 UTF-8 CSV 和 BOM CSV。
- 表头缺失、顺序错误、未知列和空文件。
- 空行、超 100 行、超文件大小。
- 公司名、税号、金额的边界值。
- 同批次完全重复行。
- 金额 `0.01`、最大整数位、超过 2 位小数。
- 解析器保留正确的原始行号。

### 10.2 后端单元测试

- 批量创建 1 条和 100 条成功。
- 空 items、101 条、空字段和错误格式被拒绝。
- 同批次重复行被拒绝。
- 事务中任意插入异常时全部回滚。
- 相同幂等键和相同 payload 返回原结果。
- 相同幂等键和不同 payload 返回 `40902`。
- 并发相同幂等键只生成一个批次。
- 普通用户无法指定其他 userId。
- 限流超过阈值返回 `42902` 和 `Retry-After`。

### 10.3 Controller/MockMvc 测试

- 未登录返回 401。
- 缺少或格式错误的 `Idempotency-Key` 返回 400。
- 正常 JSON 返回统一响应结构。
- 行级校验错误返回 HTTP 422 和错误明细。
- 超过批次数量限制返回 HTTP 400。
- 数据库冲突和系统异常返回统一错误，不泄露内部细节。

### 10.4 Playwright 端到端测试

- 用户下载模板并成功导入两行申请。
- 用户选择含错误行的文件，页面显示错误且确认按钮不可用。
- 用户提交成功后列表新增对应的 `PENDING` 记录。
- 重复点击确认不会产生重复申请。
- 网络失败后复用同一批次重试，最终只生成一批记录。
- 管理员登录后能看到批量创建的申请，并仍可逐张上传图片。
- 移动端或窄窗口下导入弹窗和预览表不发生横向布局错乱。

## 11. 分阶段实施顺序

### 阶段 1：接口和数据契约

- [ ] 定稿模板字段、行数、文件大小和错误码。
- [ ] 定稿批次请求、响应和行错误 JSON 结构。
- [ ] 新增批次表、invoice 批次关联字段的 Flyway 迁移设计。
- [ ] 确认前端解析后传 JSON，而不是把原始 Excel 直接交给后端。

### 阶段 2：后端批量创建

- [ ] 新增 DTO、entity、mapper 和数据库迁移。
- [ ] 抽取单条/批量共用的字段标准化和校验逻辑。
- [ ] 实现批次 hash、幂等查询、事务批量创建。
- [ ] 增加 controller、限流和结构化错误处理。
- [ ] 完成 Service、Mapper、Controller 测试。

### 阶段 3：前端文件导入

- [ ] 安装 `xlsx` 并实现 CSV/XLSX 解析工具。
- [ ] 增加模板下载功能。
- [ ] 增加批量导入弹窗、预览表和行级错误展示。
- [ ] 接入批量 API、提交状态和结果展示。
- [ ] 完成前端单元测试和构建检查。

### 阶段 4：联调与端到端验证

- [ ] 验证真实 MySQL Flyway 迁移和回滚行为。
- [ ] 验证普通用户权限、限流、重复提交和网络重试。
- [ ] 验证管理员列表和逐张图片上传不受影响。
- [ ] 运行 Playwright 完整流程。
- [ ] 更新 README、开发计划和 API 文档。

### 阶段 5：上线前检查

- [ ] 配置批次限制和请求大小上限。
- [ ] 检查日志中不输出完整税号和原始文件内容。
- [ ] 对 100 行批量请求进行接口压测。
- [ ] 验证批量导入失败时数据库无残留发票记录。
- [ ] 验证备份与恢复后批次和发票关联关系完整。

## 12. 验收标准

功能验收：

- 用户能通过 CSV/XLSX 一次创建最多 100 条发票申请。
- 模板字段错误、数据错误和重复行均能定位到原始行号。
- 存在错误行时不会创建任何申请。
- 成功批次的所有发票状态均为 `PENDING`。
- 管理员仍然可以对每条记录逐张上传图片，状态正常变为 `COMPLETED`。

可靠性验收：

- 同一批次重复提交不会产生重复数据。
- 同一幂等键提交不同内容会被拒绝。
- 数据库异常导致事务回滚，不产生半批次数据。
- 超过行数、请求大小或限流阈值时快速失败。

质量验收：

- 后端批量相关测试全部通过。
- 前端单元测试、`npm run build` 和 Playwright 流程通过。
- Flyway 迁移可在已有数据库上升级，`init.sql` 与新结构保持一致。
- README 和开发计划准确说明批量导入格式、限制及重试规则。

## 13. 预计工作量与风险

预计总工作量：3～5 个开发日，取决于现有数据库环境和 Excel 解析兼容性。

主要风险：

- Excel 单元格可能把税号解析成数字并丢失前导零；解析器需要优先按字符串读取，并在模板说明中要求税号单元格设为文本。
- 金额不能使用 JavaScript 浮点数直接计算；前端展示和后端金额计算均使用字符串/十进制定点规则。
- 批量幂等需要独立批次记录，不能复用单条发票的唯一幂等键。
- 前端显示提交失败不代表后端未提交，必须保留同一幂等键以支持安全重试。
- 数据量扩大后，现有“用户列表一次性返回全部发票”可能成为性能瓶颈；本功能完成后应继续评估发票列表分页。

## 14. 推荐实施结论

按照“前端解析预览 + 后端 JSON 批次接口 + 批次表幂等 + 全批事务提交”的方案实施。该方案改动边界清晰，能够复用当前单条申请的字段校验和权限体系，同时保证批量导入不会绕开安全校验，也不会影响管理员逐张上传发票图片的现有流程。
