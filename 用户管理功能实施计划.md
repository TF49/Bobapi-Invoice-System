# 用户管理功能实施计划

## Summary

完成管理员用户管理闭环：查询、筛选、分页、创建、角色调整、密码重置、启用/禁用及即时凭证失效。保留公开注册，不提供用户删除，避免破坏历史发票归属。

## Backend Changes

- 为 `user` 表增加 `enabled TINYINT DEFAULT 1` 和 `auth_version BIGINT DEFAULT 0`；通过可重复执行的条件迁移兼容现有数据库。
- JWT 增加 `authVersion`；过滤器每次鉴权回查用户，以数据库中的当前角色、状态和版本建立权限。禁用、角色变更或密码重置后递增版本，使全部旧 token 立即返回 401。
- 引入 MyBatis-Plus 3.5.9 对应的 `mybatis-plus-jsqlparser`，启用服务端分页并限制单页最多 100 条。
- 新增管理员接口，统一返回现有 `ApiResponse`：
  - `GET /api/users/admin`：参数 `page`、`pageSize`、`keyword`、可选 `role`、可选 `enabled`；返回用户分页结果及总用户、启用、禁用、管理员统计。
  - `POST /api/users/admin`：创建 USER 或 ADMIN，复用现有用户名和密码规则。
  - `PUT /api/users/admin/{id}/role`：调整角色。
  - `PUT /api/users/admin/{id}/status`：启用或禁用。
  - `PUT /api/users/admin/{id}/password`：设置新密码。
- 用户响应只包含 `id/username/role/enabled/createdAt/updatedAt/self`，绝不返回密码或认证版本。
- 所有接口仅允许 ADMIN；列表每管理员每分钟 120 次，普通写操作 30 次，密码重置 10 次。
- 角色和状态变更使用事务及数据库行锁保护“至少一个启用管理员”约束；管理员不能修改自己的角色或状态，但可以重置自己的密码。
- 用户名唯一冲突返回 409，用户不存在返回 404，自身操作或最后管理员约束返回 403/409，参数错误返回 400，限流返回 429。
- 创建依赖用户名唯一索引防重；角色和状态接口按目标状态实现自然幂等，不新增额外幂等表。

## Frontend Changes

- 新增 `/admin/users` 管理路由、用户管理 API 和 TypeScript 类型；保持 `/admin` 为发票管理。
- 在管理员页头加入“发票管理 / 用户管理”分段导航，普通用户不显示。
- 用户管理页包含四项统计、用户名搜索、角色与状态筛选、刷新、服务端分页和创建用户入口。
- 表格展示用户名、角色、状态和创建时间；支持角色调整、状态切换和密码重置，并为敏感操作提供确认对话框。
- 当前管理员行禁用角色和状态操作并显示原因；重置自己的密码成功后主动清除本地凭证并跳转登录页。
- 所有提交按钮具备独立 loading、防重复提交、成功反馈和现有统一错误处理；适配桌面及移动端横向表格。
- 更新 README 和开发计划，以实际测试数量和完成状态为准；保留现有 `AuthShell.vue` 用户改动及无关 `.playwright-cli` 内容。

## Test Plan

- 后端覆盖分页筛选、创建与密码加密、用户名冲突、角色和状态更新、禁用登录、认证版本失效、自身操作限制及并发下最后一个管理员保护。
- 使用 MockMvc 验证 ADMIN 成功、USER 访问 403、无 token 访问 401，以及 400/404/409/429 响应。
- 前端补充 API 参数、表单校验、分页筛选、确认弹窗、自身账号限制和重置自己密码后退出的组件测试。
- 在本机 MySQL 上验证迁移首次执行及重复执行，随后运行 `mvn test`、`npm test`、`npm run build`。
- 启动后端与前端，在 `http://localhost:8080` 完成真实浏览器回归：创建账号、筛选、改角色、禁用在线用户、重置密码、旧 token 失效、新密码登录，并检查桌面和移动端布局。
- 浏览器测试使用专用临时账号，确认无发票数据后精确清理，不修改现有业务账号和发票。

## Locked Decisions

- 保留现有公开注册，注册账号默认启用且角色为 USER。
- 禁用、角色变更和密码重置立即使全部旧 JWT 失效；旧格式 JWT 部署后需要重新登录。
- 管理员直接输入符合现有强度规则的新密码，不增加临时密码或首次登录强制改密流程。
- 管理员只能重置自己的密码，不能在用户管理页修改自己的角色或状态。
- 不实现删除用户、批量操作、权限细分或邮件通知。
