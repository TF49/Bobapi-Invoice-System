# 发票管理系统开发计划（按实际进度更新）

> 更新时间：2026-08-18（用户管理功能已完成）
>
> 评估依据：当前工作区代码、自动化测试和生产构建结果
>
> 状态说明：`[x]` 已完成，`[~]` 部分完成或按实际实现调整，`[ ]` 待完成

## 当前进度概览

| 优先级 | 当前状态 | 说明 |
| --- | --- | --- |
| P0 核心 MVP | 已完成 | 前后端基础设施、认证授权、发票申请和列表功能均已落地 |
| P1 文件与管理功能 | 已完成 | 发票上传、下载、管理员筛选、权限控制及用户管理全部实现；待完成真实环境端到端联调 |
| P2 优化与增强 | 大部分完成 | 异常处理、限流、幂等、全局样式和响应式布局已实现；部分交互和性能仍可优化 |
| P3 测试与部署 | 进行中 | 已有 48 个自动化测试且构建通过；缺少真实数据库端到端测试和生产部署配置 |

当前项目已达到"全部核心与管理功能开发完成、进入系统联调和交付准备"的阶段。普通用户可以注册、登录、提交发票申请、查看历史和下载已开票文件；管理员可以查看及筛选全部申请、上传发票文件、下载已上传文件，并通过专属用户管理页集中查询、创建、调整角色、重置密码和启用/禁用账号。

---

## P0 级别（核心 MVP）

### P0.1 后端基础设施

**状态：已完成**

- [x] 创建 Spring Boot 3.1.5 项目，使用 JDK 17
- [x] 配置 Spring Web、Spring Security、Validation、MyBatis-Plus、MySQL、JWT 和 Knife4j
- [x] 完成 `backend/src/main/resources/application.yml` 配置
- [x] 建立 controller、service、entity、mapper、dto、config、security、exception、utils 包结构
- [x] 通过 Flyway 版本迁移创建并升级 `user` 表和 `invoice` 表
- [x] 已有数据库使用 baseline 机制接入迁移，默认账号只首次创建且不会重置密码

### P0.2 认证授权模块

**状态：已完成**

- [x] 实现 `User` 实体和 MyBatis-Plus `UserMapper`
- [x] 实现 JWT 生成、解析和有效期处理，支持“记住我”
- [x] 实现 JWT 认证过滤器和 Spring Security 无状态鉴权
- [x] 实现登录、注册接口
- [x] 实现 USER/ADMIN 角色权限及前后端路由守卫
- [x] 实现 BCrypt 密码加密
- [x] 实现登录限流、连续失败锁定和统一登录错误提示

### P0.3 发票核心数据层

**状态：已完成**

- [x] 实现 `Invoice` 实体和 `InvoiceMapper`
- [x] 实现 `InvoiceRequest`、`InvoiceResponse` 等请求响应对象
- [x] 配置公司名称、税号、金额和幂等键校验
- [x] 增加用户与幂等键联合唯一索引，防止重复创建
- [x] 配置 MyBatis-Plus 自动填充和逻辑删除

### P0.4 发票核心业务逻辑

**状态：已完成**

- [x] `createInvoice`：用户创建发票申请
- [x] `getInvoicesByUserId`：用户查看自己的发票列表
- [x] `getAllInvoices`：管理员查看全部发票
- [x] 创建申请时支持 `Idempotency-Key`，可安全重试并检测请求体冲突
- [~] 未提供独立的状态修改接口；管理员上传文件成功后由服务自动将状态从 `PENDING` 更新为 `COMPLETED`，更符合当前业务流程

### P0.5 核心 API 接口

**状态：已完成**

- [x] `POST /api/invoices`：创建发票申请
- [x] `GET /api/invoices/my`：获取当前用户的发票列表
- [x] `GET /api/invoices/admin/all`：管理员获取全部发票
- [x] 配置仅允许本地前端开发地址访问的 CORS
- [x] 集成 Knife4j API 文档
- [x] 统一 API 响应结构、HTTP 状态码、业务错误码和 `traceId`

### P0.6 前端基础设施

**状态：已完成**

- [x] 创建 Vue 3 + TypeScript + Vite 项目
- [x] 安装 Vue Router、Pinia、Axios、Element Plus 和图标库
- [x] 配置 Vite 路径别名、开发端口和 `/api` 代理
- [x] 建立 views、components、api、stores、router、utils、styles 目录

### P0.7 前端基础架构

**状态：已完成（按实际实现调整）**

- [x] 配置登录、注册、普通用户和管理员路由
- [x] 配置用户 Pinia Store，管理 token、用户名和角色
- [x] 配置 Axios 请求/响应拦截器、JWT 注入和统一错误提示
- [x] 封装认证和发票 API 模块
- [~] 发票数据由页面本地状态配合 API 管理，当前业务规模下未单独创建 invoice store

### P0.8 认证页面

**状态：已完成**

- [x] 实现登录页面和注册页面
- [x] 使用 Element Plus 表单组件并配置前端校验
- [x] 实现登录、注册、token 存储、记住登录状态和退出登录
- [x] 实现未登录跳转和角色路由保护
- [x] 实现认证请求的限流错误及 `Retry-After` 提示处理

### P0.9 用户发票申请页面

**状态：已完成**

- [x] 实现公司名称、税号、开票金额表单
- [x] 实现税号规范化、金额范围和必填校验
- [x] 实现幂等提交及网络失败后安全重试
- [x] 实现发票历史、状态统计和完成金额展示
- [x] 实现已开票文件下载

---

## P1 级别（文件与管理功能）

### P1.1 文件上传下载（后端）

**状态：已完成（按实际实现调整）**

- [x] 配置单文件和单请求最大 10 MB
- [x] 实现管理员上传和鉴权下载接口
- [x] 实现本地文件存储目录初始化及 UUID 存储文件名
- [x] 校验文件大小、扩展名、MIME、文件头、真实解码格式、尺寸和总像素数
- [x] 防止路径穿越并清理数据库更新失败时产生的文件
- [~] 文件逻辑集中在 `InvoiceService`，未拆分独立 `FileController` 或云存储接口
- [~] 未配置静态资源映射；文件只能经鉴权接口下载，这是当前安全设计

### P1.2 文件上传下载接口

**状态：已完成**

- [x] `POST /api/invoices/admin/{id}/upload`：管理员上传 JPG、JPEG 或 PNG 图片
- [x] `GET /api/invoices/{id}/download`：发票所属用户或管理员下载文件

### P1.3 管理员发票管理页面

**状态：已完成**

- [x] 展示全部发票申请和汇总统计
- [x] 支持全部、待开票、已开票状态筛选
- [x] 支持发票文件上传，包含类型和大小前端校验
- [x] 上传成功后刷新列表并自动展示完成状态
- [x] 支持管理员下载已上传文件

### P1.4 用户下载发票功能

**状态：已完成**

- [x] 仅对可下载的已开票记录显示下载入口
- [x] 从响应头解析 UTF-8 文件名并保存 Blob
- [x] 后端校验发票归属，普通用户无法下载他人的文件

### P1.5 用户管理

**状态：已完成**

- [x] 扩展用户数据模型，增加 `enabled` 和 `auth_version` 字段；登录时拒绝已禁用账号，JWT 过滤器每次鉴权回查数据库以即时使旧凭证失效
- [x] 实现管理员用户列表接口，支持分页及按用户名、角色、账号状态筛选，服务端最大 100 条/页
- [x] 实现管理员创建用户、调整角色、重置密码和启用/禁用账号接口，复用现有用户名和密码规则
- [x] 所有用户管理接口仅允许 ADMIN 访问，并校验用户名唯一性、角色值和密码强度；按操作类型分别限流
- [x] 增加安全约束：管理员不能修改自己的角色或状态，系统至少保留一个启用的管理员（数据库行锁事务保护）
- [x] 禁用账号、角色变更或密码重置后递增 auth_version，使全部旧 JWT 立即返回 401；不删除用户，保留历史发票归属
- [x] 封装前端用户管理 API（src/api/user.ts），新增 /admin/users 路由及页头分段导航（仅 ADMIN 可见）
- [x] 实现用户管理页面，包含四项统计、用户名搜索、角色与状态筛选、服务端分页、创建用户、角色调整、密码重置和状态切换
- [x] 对角色调整、密码重置和账号禁用等敏感操作增加确认对话框；重置自己密码后清除本地凭证并跳转登录
- [x] 补充用户管理服务、接口权限、账号状态及管理员安全约束的自动化测试（9 个服务测试 + 6 个控制器测试 + 4 个前端组件测试 + 2 个 API 单元测试）

### P1.6 前后端联调

**状态：部分完成**

- [x] 登录、注册、JWT 注入和角色路由流程已接通
- [x] 发票申请、列表、上传和下载调用链已接通
- [x] 后端限制普通用户访问管理员接口
- [x] 用户管理接口与前端页面代码已完整实现，待启动真实环境完成联调验证
- [ ] 使用真实 MySQL 和浏览器完成一轮完整端到端回归（含用户管理所有操作）
- [ ] 补充 token 过期后的刷新机制，或明确采用“过期后重新登录”的产品策略

---

## P2 级别（优化与增强）

### P2.1 异常处理、安全和校验

**状态：已完成**

- [x] 实现后端全局异常处理和一致的业务错误响应
- [x] 实现前端统一错误提示、登录失效处理和限流倒计时提示
- [x] 实现前后端双重参数及文件校验
- [x] 实现请求 `traceId` 生成、响应透传和服务端错误日志关联
- [x] 实现登录及发票创建限流
- [x] 实现发票创建幂等处理

### P2.2 用户体验优化

**状态：部分完成**

- [x] 添加页面、表格、提交和上传加载状态
- [x] 优化表单校验、税号输入、统计信息和空状态
- [x] 增加页面进入、数字统计、聚光卡片和背景网格等轻量动效
- [x] 完成主要页面的移动端和窄屏适配
- [ ] 根据真实浏览器联调结果继续修复小屏表格、长用户名和长公司名称等边界场景
- [ ] 评估上传前确认、重复操作反馈和无障碍键盘操作

### P2.3 通用组件、样式和性能

**状态：部分完成**

- [x] 实现 `AppHeader`、`AuthShell` 和通用动效组件
- [x] 实现全局 CSS 变量、主题样式和工作台布局
- [x] 实现响应式布局、统一加载状态和错误提示
- [~] 原计划中的单一 `Layout.vue` 已由 `AppHeader` 与页面工作台布局组合替代
- [ ] 优化生产包体积；当前构建主 JavaScript 包约 1.19 MB，超过 Vite 500 kB 警告阈值
- [ ] 按业务规模评估发票列表分页、服务端筛选和大数据量加载策略

---

## P3 级别（测试与部署）

### P3.1 功能与自动化测试

**状态：部分完成**

- [x] 后端已有 33 个自动化测试，覆盖发票幂等、文件校验、下载权限、限流、异常响应、JWT 过滤器、用户管理服务和控制器
- [x] 前端已有 15 个自动化测试，覆盖请求错误处理、文件下载、用户 API、表单校验和 AdminUsers 组件交互
- [x] 2026-08-18 执行 `mvn test`：33 个测试全部通过
- [x] 2026-08-18 执行 `npm test`：15 个测试全部通过
- [ ] 使用独立测试数据库覆盖注册、登录、申请、上传、下载和越权访问
- [ ] 增加 Playwright 端到端测试，覆盖普通用户、发票管理员和用户管理员完整流程

### P3.2 构建与部署准备

**状态：部分完成**

- [x] 2026-08-18 执行 `npm run build`，前端生产构建成功
- [x] 2026-08-18 执行 `mvn package`，后端可执行 JAR 打包成功
- [x] README 已提供本机环境要求、默认账号、启动命令、API 约定和文件存储说明
- [ ] 使用生产式配置独立启动后端 JAR 并执行健康检查
- [x] 数据库密码、JWT 密钥、上传目录和 CORS 域名支持环境变量或生产配置，秘密参数无源码默认值
- [ ] 修正 README 中数据库密码示例与 `application.yml` 当前值不一致的问题
- [x] 使用 Flyway 管理生产数据库迁移，不再依赖启动时重复执行 `schema.sql`
- [ ] 增加上传文件备份、清理、容量监控和云存储切换方案
- [ ] 配置反向代理、HTTPS、日志归档、健康检查和部署说明
- [ ] 增加 CI 流程，自动执行后端测试、前端测试和生产构建

---

## 下一阶段执行顺序

1. **真实环境端到端联调**：启动 MySQL、后端和前端，回归普通用户注册登录、提交申请、管理员上传、用户下载、用户管理（创建/角色/禁用/密码重置）和越权拦截；验证禁用账号后旧 token 立即返回 401。
2. **补齐端到端测试**：优先覆盖核心业务闭环、用户管理和认证权限，考虑增加 Playwright 浏览器测试。
3. **完成生产配置治理**：移除源码中的数据库密码和固定 JWT 密钥，配置生产 CORS、上传目录及数据库迁移。
4. **验证后端交付物**：打包 JAR，使用生产式配置独立启动并执行健康检查。
5. **处理性能与运维事项**：拆分前端大包（当前约 1.19 MB）、增加文件备份监控和 CI/CD。

---

## 技术栈现状

**后端**

- Spring Boot 3.1.5 + JDK 17
- Spring Security + JWT + BCrypt
- MyBatis-Plus 3.5.9 + MySQL 8
- Jakarta Validation + Knife4j 4.4.0
- JUnit 5 + Mockito + MockMvc

**前端**

- Vue 3.4 + TypeScript 5.3 + Vite 5
- Element Plus + Vue Router + Pinia + Axios
- Vitest

**当前核心业务闭环**

1. 普通用户注册或登录并提交发票申请。
2. 管理员查看申请并上传开具完成的发票文件。
3. 系统自动更新发票状态，普通用户下载自己的已开票文件。
