# 用户充值与开票手续费开发计划

## 一、需求结论

参考 [NewAPI 配额与充值](https://www.newapi.ai/zh/docs/guide/feature-guide/user/topup) 的“创建订单—跳转支付—异步通知—余额到账—查询结果”流程，为现有系统增加人民币额度账户。

- 充值额度与人民币按 `1:1` 换算，支付 100 元到账 100.00 元。
- 一期接入 EPay 聚合支付，支持支付宝、微信，并提供兑换码充值。
- Stripe、Creem、Waffo、邀请返利和订阅套餐不在一期范围。
- 开票手续费永久固定为 `3%`，按 `开票金额 × 3%` 四舍五入到分。
- 单笔申请提交成功时立即扣除手续费；余额不足则不创建申请。
- 批量申请按每张发票分别计算手续费后求和，余额不足则整批不创建、不扣费。
- 待开票申请允许用户取消、管理员拒绝，并自动把手续费退回额度账户。
- 已完成发票不能取消或拒绝；退款仅退回系统额度，不发起 EPay 原支付退款。
- 示例：充值 10 元后余额 10 元，申请 100 元发票扣除 3 元，余额变为 7 元；取消申请后余额恢复为 10 元。

## 二、资金模型与核心业务实现

### 2.1 数据库与迁移

新增 Flyway 增量迁移，不修改已执行的 V1–V4，并同步维护手工初始化脚本。

- `wallet_account`：用户唯一账户，保存 `balance DECIMAL(14,2)`、更新时间；余额必须大于等于 0。
- `wallet_transaction`：不可修改的资金流水，记录流水号、用户、带符号金额、变动前后余额、类型、业务编号、备注和时间。
  - 类型包含 `RECHARGE`、`REDEMPTION`、`INVOICE_FEE`、`INVOICE_REFUND`。
  - 对“流水类型 + 业务编号”建立唯一约束，防止重复入账或退款。
- `recharge_order`：充值订单号、用户、金额、EPay 支付类型、状态、幂等键、请求哈希、第三方交易号、过期和支付时间。
  - 状态为 `PENDING`、`PAID`、`CLOSED`、`FAILED`。
- `redemption_code`：兑换码哈希、掩码前缀、金额、状态、过期时间、使用用户和使用时间；数据库不保存明文兑换码。
- `invoice` 增加 `fee_rate`、`fee_amount`、`fee_status`、取消/拒绝原因和终止时间。
  - 状态扩展为 `PENDING`、`COMPLETED`、`CANCELLED`、`REJECTED`。
  - 费用状态为 `LEGACY_NO_CHARGE`、`DEDUCTED`、`REFUNDED`。
- `invoice_batch` 增加批次总手续费。
- 为现有用户初始化 0 元账户；历史发票统一标记为 `LEGACY_NO_CHARGE`，包括历史待处理发票，不追补手续费。

### 2.2 原子扣费与退款

- 单笔和批量创建改为数据库事务，统一锁定用户账户后执行余额校验、发票创建、余额更新和流水写入。
- 通过账户行锁、非负约束、业务唯一索引和既有 `Idempotency-Key` 共同保证并发下不透支、不重复扣款。
- 批量申请在同一事务内创建全部发票并写入逐发票手续费流水，任一环节失败整批回滚。
- 取消或拒绝时按固定锁顺序读取账户和发票，执行条件状态更新，并写入唯一退款流水；重复请求返回既有结果，不重复退款。
- 管理员上传文件与取消/拒绝并发时，仅一个状态转换能够成功；失败上传产生的临时文件继续按现有逻辑清理。
- 手续费为 0.00 时仍保存费用快照和零金额审计流水，保证完整可追踪。

### 2.3 EPay 与兑换码

- 增加支付网关接口，先实现 EPay 兼容适配器，后续国际支付可新增实现而不改钱包逻辑。
- EPay 使用商户号、网关地址和密钥生成支付地址；参数包含商户订单号、支付类型、金额、通知地址和固定回跳地址。
- 异步通知同时支持 EPay 的 GET/表单回调，校验签名、商户号、支付状态、订单号、金额和第三方交易号。
- 只有验签通过的异步通知可以入账；浏览器回跳页只查询订单状态，绝不直接修改余额。
- 重复或并发回调对已经支付的订单直接返回纯文本 `success`，确保只入账一次。
- 待支付订单默认 30 分钟本地过期；有效的迟到支付通知仍可转为已支付，避免用户已付款却未到账。
- 兑换码采用安全随机值和 HMAC-SHA256 哈希，一码一次；管理员批量生成时明文只展示和导出一次。
- 默认单笔充值范围为 1.00～50,000.00 元，通过环境变量调整；渠道手续费由平台承担，用户按实付金额全额到账。

## 三、接口、页面与类型调整

### 3.1 用户接口

- `GET /wallet/summary`：账户余额及累计充值、累计消费。
- `GET /wallet/transactions`：分页查询本人资金流水。
- `POST /wallet/redemptions`：兑换码充值，要求 `Idempotency-Key`。
- `GET /payments/methods`：返回当前启用的支付宝、微信渠道。
- `POST /payments/orders`：创建充值订单，参数为金额和支付方式，要求 `Idempotency-Key`，返回订单号、支付地址和过期时间。
- `GET /payments/orders/my`、`GET /payments/orders/{orderNo}`：查询本人充值订单。
- `GET|POST /payments/epay/notify`：无需 JWT，但必须通过 EPay 验签。
- `POST /invoices/{id}/cancel`：用户取消自己的待开票申请并退款。

### 3.2 管理员接口

- `GET /payments/admin/orders`：按用户、订单号、渠道、状态和时间分页查询充值订单。
- `GET /wallet/admin/transactions`：查询全局资金流水。
- `POST /wallet/admin/redemption-batches`：创建固定金额、数量及有效期的兑换码批次。
- `GET /wallet/admin/redemptions`：查询兑换码状态。
- `POST /wallet/admin/redemptions/{id}/disable`：禁用尚未使用的兑换码。
- `POST /invoices/admin/{id}/reject`：填写拒绝原因并自动退回手续费。
- 管理员用户列表响应增加当前余额，但不提供直接覆盖余额的接口，所有余额变化必须产生流水。

### 3.3 公共类型与前端

- `InvoiceResponse` 增加 `feeRate`、`feeAmount`、`feeStatus`、`terminationReason`。
- 批量申请响应增加 `totalFee` 和 `remainingBalance`。
- 新增 `WalletSummary`、`WalletTransaction`、`RechargeOrder`、`PaymentMethod`、`RedemptionCode` 类型。
- 用户端增加“发票管理/钱包充值”导航和 `/user/wallet` 页面：
  - 展示余额、快捷金额、自定义金额、支付宝/微信、充值订单、资金流水及兑换码入口。
  - 支付回跳页每 2 秒查询订单，最多 60 秒，超时后提供手动刷新。
- 开票表单实时展示固定费率、预计手续费、当前余额及扣费后余额；前端余额不足时禁用提交，后端仍做权威校验。
- 发票列表展示手续费，并为待开票记录提供取消按钮和退款确认提示。
- 管理员增加“资金管理”页面，包含充值订单、资金流水和兑换码管理；发票页增加拒绝操作。

## 四、接口防护与异常策略

按照接口防护六道防线落实：

- 参数校验：金额仅允许两位小数、白名单支付方式、充值范围、兑换码格式和拒绝原因长度。
- 业务校验：账户和订单存在、余额充足、状态转换合法、回调金额与订单完全一致。
- 权限校验：用户只能查看自己的账户、订单和发票；资金管理与兑换码接口仅限管理员。
- 幂等处理：所有用户写操作使用 `Idempotency-Key`；回调、扣费、充值和退款再由数据库唯一索引兜底。
- 限流：充值下单默认用户 5 次/分钟、IP 20 次/分钟；兑换失败默认用户/IP 5 次/分钟并限制连续失败；回调采用较高阈值且验签优先。
- 熔断与异常：EPay 通信设置连接和读取超时，不盲目创建重复订单；第三方故障不影响开票与余额查询。
- 余额不足返回 HTTP 422；幂等键对应不同请求返回 409；非法签名、金额不符和越权分别返回明确错误。
- 日志只记录订单号、流水号、发票号和 `traceId`，严禁记录商户密钥、完整兑换码或回调签名原文。

## 五、开发顺序与验收

### 5.1 开发顺序

1. 完成数据库迁移、钱包账户、不可变流水及金额计算组件。
2. 重构单笔/批量开票为原子扣费流程，并实现取消、拒绝和退款状态机。
3. 实现充值订单、EPay 签名适配器、异步回调、订单查询和过期处理。
4. 实现兑换码生成、禁用、兑换与审计。
5. 完成用户钱包页、开票费用提示、管理员资金页和类型/API 适配。
6. 补齐自动化测试、对账检查、部署配置与操作文档。

### 5.2 测试与验收

- 100 元发票收取 3.00 元；边界金额按 `HALF_UP` 保留两位。
- 余额刚好、余额不足、零手续费舍入、批量逐项取整后求和。
- 相同幂等请求不重复扣费，不同请求体复用幂等键返回冲突。
- 多线程同时开票不会产生负余额；批量失败没有残留发票、余额或流水。
- 用户取消、管理员拒绝、重复退款及上传与取消并发均只发生一次状态转换。
- 正常、重复、并发、非法签名、金额篡改、未知订单和迟到 EPay 回调。
- 同一兑换码并发兑换只有一个用户成功，禁用和过期兑换码不能使用。
- 用户无法访问他人订单、钱包和发票，普通用户无法访问资金管理接口。
- 迁移后历史发票不扣费、现有用户余额为 0，新建用户自动创建钱包。
- 前端 Vitest 覆盖金额展示、支付状态轮询和按钮权限；Playwright 覆盖“充值到账—申请扣费—取消退款”主流程。
- 后端使用 MySQL 集成测试验证行锁和唯一约束，最终执行 `mvn test`、`npm test`、`npm run build`。

## 六、上线假设与配置

- 币种固定为人民币 CNY，所有金额使用 `BigDecimal/DECIMAL`，禁止使用浮点数。
- 系统仍按当前单节点部署；若改为多节点，现有内存限流必须先替换为 Redis 全局限流。
- 上线前必须取得兼容 EPay 协议的网关地址、商户号、商户密钥和已开通的支付宝/微信渠道。
- 必须配置公网 HTTPS 回调域名、前端回跳域名、兑换码哈希密钥及充值上下限；密钥只能通过环境变量注入。
- 首次发布先关闭 EPay 功能开关，完成数据库备份、迁移、沙箱回调与对账验证后再开放支付按钮。
- 每日校验“账户余额 = 全部流水金额之和”“已支付订单恰好对应一条充值流水”“已退款发票恰好对应一条退款流水”；发现不一致立即告警并暂停充值入口。
