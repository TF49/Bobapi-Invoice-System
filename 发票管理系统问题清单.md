# 发票管理系统问题清单

> 涂夫哥
>
> 更新时间：2026-08-18（二次修订）
>
> 状态：P0/P1/P2 原有问题已全部修复；新发现的安全和稳定性问题已修复或记录。
>
> 范围：用户提交开票申请、管理员上传已开具发票、用户预览和下载发票图片的完整链路。

## 一、验收目标

目标业务流程应为：

1. 普通用户提交公司名称、税号和金额。
2. 管理员在申请记录的"操作"列直接粘贴发票图片，也可以保留文件选择作为备用入口。
3. 上传成功后，申请状态变为"已开票"。
4. 普通用户在自己的"操作"列查看发票图片，并可下载原始图片文件。
5. 普通用户不能查看或下载其他用户的发票，管理员可以处理全部申请。

✅ 上述目标已全部实现。

## 二、P0 功能问题

### ✅ P0-1 管理员操作列不支持剪贴板粘贴图片

**状态：已修复**

已在 `AdminInvoice.vue` 的待处理申请操作区域实现：
- `paste-zone` 可聚焦区域监听 `paste` 事件（`handlePaste`）
- 从 `ClipboardEvent.clipboardData.items` 和 `.files` 提取 `image/jpeg`、`image/png` 内容
- 剪贴板图片转换为 `File` 后复用 `invoiceApi.uploadInvoice()` 上传
- 粘贴非图片内容时弹出提示，不发送请求
- "选择文件"仍作为备用入口保留

### ✅ P0-2 用户操作列没有图片预览

**状态：已修复**

- 后端新增 `GET /invoices/{id}/preview` 接口，返回 `Content-Disposition: inline`，带 `X-Content-Type-Options: nosniff` 响应头
- 前端 `UserInvoice.vue` 新增"查看"按钮，调用 `invoiceApi.previewInvoice()`，生成 Blob URL 后在 `el-dialog` 中用 `<img>` 展示
- 弹窗关闭时调用 `URL.revokeObjectURL()` 释放内存
- 同步支持 AbortController 取消进行中的预览请求
- "查看"和"下载"保持两个独立操作

## 三、P1 安全和业务问题

### ✅ P1-1 系统仍允许 PDF，与图片-only 需求不一致

**状态：已修复**

- 后端 `AllowedFileType` 枚举只保留 `JPEG` 和 `PNG`，无 PDF 分支
- 前端 `accept=".jpg,.jpeg,.png"`，MIME 白名单只含 `image/jpeg`、`image/jpg`、`image/png`
- README 和错误提示均已更新为"仅支持 JPG、JPEG、PNG"

### ✅ P1-2 图片校验只检查文件头，没有实际解码和尺寸限制

**状态：已修复**

- `InvoiceService.validateDecodedImage()` 使用 `ImageIO.getImageReaders()` 真实解码
- 校验 `FormatName`、图片宽/高、总像素数（超限返回 `413`）
- 读取 `BufferedImage` 确认内容完整，损坏文件返回 `400`
- 宽、高、像素上限通过环境变量 `INVOICE_IMAGE_MAX_WIDTH/HEIGHT/PIXELS` 配置化
- `preview` 和 `download` 接口均带 `X-Content-Type-Options: nosniff`

### ✅ P1-3 默认账号密码会在后端重启时被重置

**状态：已修复**

- `application.yml` 配置 `spring.sql.init.mode: never`，启动时不执行 SQL
- `schema.sql` 改用 `INSERT IGNORE`，不覆盖已修改的密码（文件已加废弃注释，不再被执行）
- Flyway 接管数据库版本管理，迁移脚本在 `db/migration/`，`INSERT IGNORE` 同样保护已有数据

### ✅ P1-4 数据库密码和 JWT 密钥硬编码在配置文件中

**状态：已修复**

- `DB_PASSWORD` 和 `JWT_SECRET` 无默认值，不注入则启动失败
- README 只保留变量名示例，不含真实密码
- CORS 域名、上传路径、图片限制全部支持环境变量覆盖

## 四、P2 稳定性和测试问题

### ✅ P2-1 文件上传和下载没有独立限流

**状态：已修复**

- 上传：用户维度 20次/5分钟 + IP 维度 40次/5分钟
- 预览/下载：用户维度 120次/分钟 + IP 维度 240次/分钟 + 单发票维度 30次/分钟
- IP 提取已改用 `WebUtils.extractClientIp()`，支持反向代理场景（见 Fix-E）

### ✅ P2-2 `downloadable` 没有确认物理文件存在

**状态：已修复**

- `InvoiceResponse.from(invoice, uploadRoot)` 通过 `Files.isRegularFile()` 检查物理文件
- `downloadable` 和 `fileExists` 均依赖物理文件是否实际存在
- 文件缺失时前端操作列显示"暂不可用"

### ⚠️ P2-3 缺少真实双角色端到端测试

**状态：待修复（规划中）**

当前自动化测试覆盖了服务层（InvoiceService、UserService）、工具函数（RateLimitService、JwtFilter）和控制器（MockMvc），但尚无完整覆盖以下流程的 Playwright 测试：

- 普通用户登录并提交申请
- 管理员查看申请并粘贴图片
- 上传完成后普通用户刷新列表并预览图片
- 普通用户越权访问管理员接口或其他用户文件

建议增加一条 Playwright 流程，使用独立测试数据库和临时上传目录，测试结束后自动清理数据。

## 五、新发现问题

### ✅ Fix-A 注册接口缺少限流

**状态：已修复**（本轮）

- `AuthController.register()` 按客户端 IP 限制 **5 次 / 分钟**，超限返回 `429`，错误码 `42906`
- 复用已有 `RateLimitService`

### ✅ Fix-E 限流和锁定未读取反向代理 IP 头

**状态：已修复**（本轮）

- 新建 `WebUtils.extractClientIp(HttpServletRequest)`，优先读 `X-Forwarded-For` 第一段，回退 `X-Real-IP`，最后才用 `remoteAddr`
- `AuthController`（登录、注册）和 `InvoiceController`（上传、预览、下载）全部改用 `WebUtils.extractClientIp()`

> [!WARNING]
> 生产环境必须在 Nginx 侧配置 `proxy_set_header X-Real-IP $remote_addr`，否则客户端可伪造 `X-Forwarded-For` 绕过 IP 限流。

### ✅ Fix-G 管理员端已完成发票没有预览

**状态：已修复**（本轮）

- `AdminInvoice.vue` 操作列已完成状态新增"查看"按钮，操作列宽度调整为 250px
- 复用 `invoiceApi.previewInvoice()` 和与 `UserInvoice.vue` 相同的 Blob URL + `el-dialog` 预览逻辑
- 弹窗含加载中、图片、错误三种状态，关闭时释放 Blob URL

### ✅ Fix-H 上传失败时错误信息未展示给用户

**状态：已修复**（本轮）

- `handleUpload` 的 `catch` 块改为 `throw error`，让 Axios 响应拦截器（`request.ts`）的 `ElMessage.error(message)` 正常触发
- 拦截器已实现从 `error.response.data.message` 提取后端返回的具体错误原因（如"只支持 JPG、JPEG 和 PNG 图片文件"、"图片内容不完整或已损坏"）

### ✅ Fix-F `schema.sql` 残留未加废弃说明

**状态：已修复**（本轮）

- `schema.sql` 顶部加明确注释：文件已废弃，Flyway 接管，不可手动执行

### 📝 已知权衡-B 注册无身份验证机制

**状态：产品设计决策，不修改**

注册后立即颁发 JWT，无邮箱/手机号验证。对内部系统属于合理简化；若对外开放则建议增加验证机制。

### 📝 已知权衡-C 内存限流/锁定状态重启清零

**状态：已知限制，文档记录**

`LoginAttemptService` 和 `RateLimitService` 均基于 `ConcurrentHashMap`，后端重启后状态清空。单机部署下属于合理简化，生产多实例部署或需要持久化锁定时应引入 Redis。

### 📝 已知权衡-D JWT 验证每次请求查询数据库

**状态：已知权衡，文档记录**

`JwtAuthenticationFilter` 每次请求执行 `userMapper.selectById()` 以校验 `authVersion`，保证 token 强制失效的实时性（修改密码、禁用账号后旧 token 立即失效）。如需优化性能，可引入 Caffeine 短 TTL 缓存（30 秒），但会带来最多 30 秒的失效延迟。

## 六、接口防护六道防线审查

| 防线 | 当前状态 | 说明 |
| --- | --- | --- |
| 参数校验 | ✅ 完备 | 必填、长度、金额、扩展名、MIME、文件头、真实解码格式、图片尺寸和总像素全部校验，已限 JPG/PNG |
| 业务规则校验 | ✅ 完备 | 只有 `PENDING` 发票可以上传；数据库更新使用状态条件避免重复完成 |
| 权限校验 | ✅ 完备 | 管理员接口要求 `ADMIN`；预览/下载校验发票归属用户或管理员身份 |
| 幂等处理 | ✅ 基本完备 | 创建申请使用 `Idempotency-Key`；上传依靠 `PENDING` 状态条件；无独立上传幂等键（当前场景可接受） |
| 请求限流 | ✅ 完备 | 登录/注册/申请/上传/预览/下载均有独立限流；IP 提取支持反向代理 |
| 异常处理 | ✅ 完备 | 统一错误响应和 HTTP 状态码；鉴权失败、文件缺失、图片损坏均有明确处理和提示 |

## 七、验证记录（本轮）

- 后端测试：`mvn test`，**33 项通过**，0 失败
- 前端测试：`npm test -- --run`，**15 项通过**，0 失败
- 已验证改动：
  - 注册接口 5次/分钟 IP 限流
  - `WebUtils.extractClientIp()` 读取 `X-Forwarded-For`
  - 管理员操作列"查看"按钮及预览弹窗
  - 上传失败时 Axios 拦截器错误弹窗
  - `schema.sql` 废弃注释

## 八、建议后续修复顺序

1. 补充双角色 Playwright 端到端测试（P2-3），覆盖登录→申请→上传→预览→越权访问完整链路。
2. 评估是否引入 Redis 持久化限流/锁定状态（权衡-C），针对多实例部署场景。
3. 评估是否对 JWT 验证引入短 TTL 本地缓存（权衡-D），针对高并发场景。
4. 评估注册流程是否需要身份验证机制（权衡-B），取决于系统对外开放程度。
